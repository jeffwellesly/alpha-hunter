# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.vercel
supabase/.temp
cli/output

# Defense-in-depth: this app takes API keys as runtime user input (stored in
# the browser's localStorage only) and has no server-side secrets by design -
# but never commit env files if one gets added later for local tooling.
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

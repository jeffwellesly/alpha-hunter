# Copy to .env.local for local dev, and set the same two values in Vercel
# project settings for production. Without these, the app runs exactly as it
# does today: localStorage-only Anthropic key, no accounts, no persistence -
# accounts are additive, not required.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
